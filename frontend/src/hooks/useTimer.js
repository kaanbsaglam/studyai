/**
 * useTimer Hook
 *
 * Re-export of useTimer from TimerContext for convenience.
 * Import this hook from here for cleaner imports.
 */

export { useTimer, PHASES } from '../context/TimerContext';
